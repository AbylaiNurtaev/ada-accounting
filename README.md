# JADI GROUP

React + TypeScript + Vite landing page prepared for Vercel.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Vercel settings are defined in `vercel.json`: framework `vite`, build command `npm run build`, output directory `dist`.
